# 🚀 Phase 1-2 Implementation Guide
## Database Expansion & Security Layer Deployment

**Generated**: March 19, 2026  
**Status**: Ready for Implementation  
**Estimated Duration**: 3-5 days  

---

## 📑 Table of Contents

1. [Overview](#overview)
2. [Database Migration Steps](#database-migration-steps)
3. [TypeScript Types Generation](#typescript-types-generation)
4. [Security Implementation](#security-implementation)
5. [Testing & Validation](#testing--validation)
6. [Deployment Checklist](#deployment-checklist)

---

## Overview

### What's Being Deployed

**Phase 1** creates 18 new database tables:

**Core Data Tables**:
- `plans_history` - Training plan records
- `daily_progress` - Daily activity logs
- `completed_workouts` - Exercise records
- `completed_meals` - Nutrition logs

**User Preferences**:
- `favorite_meals` - Saved favorite meals
- `favorite_exercises` - Saved exercises
- `user_achievements` - Earned badges
- `user_milestones` - Goal milestones
- `streaks` - Activity streaks
- `user_points` - Gamification scores
- `point_transactions` - Point history

**Platform Features**:
- `notifications` - In-app & push alerts
- `user_feedback` - Bug reports & suggestions
- `plan_ratings` - Plan reviews
- `leaderboard_snapshot` - Ranking data
- `audit_logs` - Security audit trail
- `data_consent` - Privacy preferences
- `data_export_requests` - GDPR data exports
- `health_metrics` - Enhanced health tracking

### Database Migrations Location

```
supabase/migrations/
├── 20260319_core_data_tables.sql          (4 tables)
├── 20260319_favorites_achievements.sql    (7 tables)
└── 20260319_notifications_admin.sql       (7 tables)
```

---

## Database Migration Steps

### Step 1: Backup Current Database

```bash
# Create a snapshot before migration
# Supabase Auto-Backup: Every 24 hours
# Manual Backup: Login to Supabase Dashboard > Settings > Backups

# Command if using Supabase CLI:
supabase projects list
supabase db pull --local  # Create local snapshot
```

### Step 2: Apply Migrations via Supabase Dashboard

```
1. Login to console.supabase.com
2. Select project: "fwjamapspffchephqqdr"
3. Go to: SQL Editor
4. Create new Query
5. Copy entire content from 20260319_core_data_tables.sql
6. Execute (verify no errors)
7. Repeat for other two migration files
```

### Step 3: Apply Migrations via Supabase CLI (Alternative)

```bash
# Install Supabase CLI (if not already installed)
npm install -g supabase

# Login to Supabase
supabase login

# Navigate to project directory
cd d:\chatbot coach\fit-coach-ai-main

# Push migrations
supabase db push

# Verify migrations applied
supabase db status
```

### Step 4: Verify Migration Success

```sql
-- Run these queries in Supabase SQL Editor to verify tables created:

SELECT table_name FROM information_schema.tables 
WHERE table_schema = 'public' 
ORDER BY table_name;

-- Check specific tables:
\d plans_history
\d daily_progress
\d favorite_meals
\d user_points
\d notifications
\d audit_logs
```

### Step 5: Generate Updated TypeScript Types

```bash
# Supabase automatically generates types from schema changes
# Method 1: Supabase Dashboard
#   Settings > API > TypeScript Docs (auto-generated)

# Method 2: CLI
supabase gen types typescript --project-id fwjamapspffchephqqdr > types/supabase.ts

# Method 3: Manual (if CLI unavailable)
# 1. Export schema from dashboard
# 2. Use supabase-js to introspect types
# 3. Save to: src/lib/database.types.ts
```

---

## TypeScript Types Generation

### Update Supabase Client Types

Create new file: `src/lib/database.types.ts`

```typescript
// This file is auto-generated by Supabase
// Command: supabase gen types typescript --project-id fwjamapspffchephqqdr

export type Database = {
  public: {
    Tables: {
      plans_history: {
        Row: {
          id: string
          user_id: string
          plan_type: 'workout' | 'nutrition' | 'combined'
          plan_content: object
          duration_days: number
          start_date: string
          end_date: string
          completion_rate: number
          created_at: string
          updated_at: string
        }
        Insert: Omit<Database['public']['Tables']['plans_history']['Row'], 'id' | 'created_at' | 'updated_at'>
        Update: Partial<Database['public']['Tables']['plans_history']['Insert']>
      }
      
      daily_progress: {
        Row: {
          id: string
          user_id: string
          log_date: string
          weight: number | null
          calories_consumed: number | null
          calories_burned: number | null
          sleep_hours: number | null
          exercise_duration: number | null
          mood: string | null
          created_at: string
        }
        Insert: Omit<Database['public']['Tables']['daily_progress']['Row'], 'id' | 'created_at'>
        Update: Partial<Database['public']['Tables']['daily_progress']['Insert']>
      }
      
      user_points: {
        Row: {
          id: string
          user_id: string
          points_total: number
          points_month: number
          level: number
          experience: number
          created_at: string
          updated_at: string
        }
        Insert: Omit<Database['public']['Tables']['user_points']['Row'], 'id' | 'created_at' | 'updated_at'>
        Update: Partial<Database['public']['Tables']['user_points']['Insert']>
      }
      
      // ... add other tables similarly
    }
  }
}
```

### Update Supabase Client Configuration

File: `src/integrations/supabase/client.ts`

```typescript
import { createClient } from '@supabase/supabase-js'
import type { Database } from '@/lib/database.types' // ADD THIS

const supabaseUrl = import.meta.env.VITE_SUPABASE_URL
const supabaseAnonKey = import.meta.env.VITE_SUPABASE_ANON_KEY

export const supabase = createClient<Database>(
  supabaseUrl,
  supabaseAnonKey
)

// Now fully typed:
// const { data: plans } = await supabase
//   .from('plans_history')
//   .select('*')
//   ✓ All fields are type-safe!
```

---

## Security Implementation

### Step 1: Update Authentication Flow

Create new file: `ai_backend/auth/jwt_manager.py`

```python
from datetime import datetime, timedelta
from typing import Optional
import jwt
import bcrypt
from pydantic import BaseModel

class JWTConfig:
    SECRET_KEY = os.getenv('JWT_SECRET_KEY', 'your-secret-key-change-in-prod')
    ALGORITHM = "HS256"
    ACCESS_TOKEN_EXPIRE_MINUTES = 30
    REFRESH_TOKEN_EXPIRE_DAYS = 7

class TokenResponse(BaseModel):
    access_token: str
    refresh_token: str
    token_type: str = "Bearer"
    expires_in: int

class TokenPayload(BaseModel):
    sub: str  # user_id
    exp: datetime
    iat: datetime
    type: str  # 'access' or 'refresh'
    role: str  # 'user', 'coach', 'admin'

class JWTManager:
    @staticmethod
    def create_access_token(user_id: str, role: str) -> str:
        """Create short-lived access token"""
        now = datetime.utcnow()
        expires = now + timedelta(minutes=JWTConfig.ACCESS_TOKEN_EXPIRE_MINUTES)
        
        payload = {
            'sub': user_id,
            'type': 'access',
            'role': role,
            'iat': now,
            'exp': expires
        }
        
        return jwt.encode(
            payload,
            JWTConfig.SECRET_KEY,
            algorithm=JWTConfig.ALGORITHM
        )
    
    @staticmethod
    def create_refresh_token(user_id: str) -> str:
        """Create long-lived refresh token"""
        now = datetime.utcnow()
        expires = now + timedelta(days=JWTConfig.REFRESH_TOKEN_EXPIRE_DAYS)
        
        payload = {
            'sub': user_id,
            'type': 'refresh',
            'iat': now,
            'exp': expires
        }
        
        return jwt.encode(
            payload,
            JWTConfig.SECRET_KEY,
            algorithm=JWTConfig.ALGORITHM
        )
    
    @staticmethod
    def verify_token(token: str) -> dict:
        """Verify and decode token"""
        try:
            payload = jwt.decode(
                token,
                JWTConfig.SECRET_KEY,
                algorithms=[JWTConfig.ALGORITHM]
            )
            return payload
        except jwt.ExpiredSignatureError:
            raise ValueError("Token expired")
        except jwt.InvalidTokenError:
            raise ValueError("Invalid token")
    
    @staticmethod
    def hash_password(password: str) -> str:
        """Hash password using bcrypt"""
        salt = bcrypt.gensalt(rounds=12)
        return bcrypt.hashpw(password.encode(), salt).decode()
    
    @staticmethod
    def verify_password(password: str, hash: str) -> bool:
        """Verify password against hash"""
        return bcrypt.checkpw(password.encode(), hash.encode())

class SessionManager:
    """Manage user sessions with Redis"""
    
    def create_session(self, user_id: str, token: str, device_id: str):
        """Create new session"""
        session_data = {
            'user_id': user_id,
            'token': token,
            'device_id': device_id,
            'created_at': datetime.utcnow()
        }
        redis.setex(f"session:{token}", 86400, json.dumps(session_data))
    
    def revoke_session(self, token: str):
        """Revoke session"""
        redis.delete(f"session:{token}")
    
    def get_active_sessions(self, user_id: str) -> List[dict]:
        """Get all active sessions for user"""
        sessions = []
        for key in redis.keys(f"session:*"):
            data = redis.get(key)
            if data and json.loads(data)['user_id'] == user_id:
                sessions.append(json.loads(data))
        return sessions
```

### Step 2: Implement API Security Headers

File: `ai_backend/config/security.py`

```python
from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware
import os

SECURITY_HEADERS = {
    "X-Content-Type-Options": "nosniff",
    "X-Frame-Options": "DENY",
    "X-XSS-Protection": "1; mode=block",
    "Strict-Transport-Security": "max-age=31536000; includeSubDomains",
    "Content-Security-Policy": "default-src 'self'; script-src 'self' cdn.jsdelivr.net",
    "Access-Control-Allow-Credentials": "true"
}

ALLOWED_ORIGINS = [
    "http://localhost:8080",
    "http://localhost:3000",
    "https://fitcoach.app",
    "https://app.fitcoach.app"
]

def setup_security(app: FastAPI):
    """Configure security headers and CORS"""
    
    # CORS Configuration
    app.add_middleware(
        CORSMiddleware,
        allow_origins=ALLOWED_ORIGINS,
        allow_credentials=True,
        allow_methods=["GET", "POST", "PUT", "DELETE", "PATCH", "OPTIONS"],
        allow_headers=["Authorization", "Content-Type"],
        max_age=3600
    )
    
    # Security Headers Middleware
    @app.middleware("http")
    async def add_security_headers(request, call_next):
        response = await call_next(request)
        for header, value in SECURITY_HEADERS.items():
            response.headers[header] = value
        return response
```

### Step 3: Implement Rate Limiting

File: `ai_backend/middleware/rate_limit.py`

```python
from fastapi import Request
from slowapi import Limiter
from slowapi.util import get_remote_address

limiter = Limiter(key_func=get_remote_address)

# Rate limit configurations
RATE_LIMITS = {
    "public": "100/hour",
    "authenticated": "1000/hour",
    "auth_endpoints": "5/minute",  # Prevent brute force
    "file_upload": "100MB",
}

# Apply to main.py:
# app.state.limiter = limiter
# app.add_exception_handler(RateLimitExceeded, _rate_limit_exceeded_handler)
```

### Step 4: Add Audit Logging

File: `ai_backend/middleware/audit.py`

```python
from datetime import datetime
from typing import Optional
import json

class AuditLogger:
    @staticmethod
    async def log_action(
        user_id: Optional[str],
        action: str,
        resource_type: str,
        resource_id: Optional[str],
        request,
        success: bool,
        old_value: Optional[dict] = None,
        new_value: Optional[dict] = None,
        error_message: Optional[str] = None
    ):
        """Log action to audit_logs table"""
        
        audit_entry = {
            'user_id': user_id,
            'action': action,
            'resource_type': resource_type,
            'resource_id': resource_id,
            'ip_address': request.client.host,
            'user_agent': request.headers.get('user-agent'),
            'http_method': request.method,
            'endpoint': str(request.url),
            'old_value': old_value,
            'new_value': new_value,
            'success': success,
            'error_message': error_message,
            'created_at': datetime.utcnow()
        }
        
        # Save to database
        await supabase.table('audit_logs').insert(audit_entry).execute()
```

---

## Testing & Validation

### Step 1: Unit Tests

Create new file: `tests/test_database_schema.py`

```python
import pytest
from supabase import create_client

@pytest.fixture
def supabase_client():
    return create_client(
        os.getenv('SUPABASE_URL'),
        os.getenv('SUPABASE_KEY')
    )

def test_plans_history_table_exists(supabase_client):
    """Verify plans_history table created correctly"""
    response = supabase_client.table('plans_history').select('*').limit(1).execute()
    assert response is not None
    assert len(response.data) == 0  # Empty initially

def test_daily_progress_table_exists(supabase_client):
    """Verify daily_progress table created"""
    response = supabase_client.table('daily_progress').select('*').limit(1).execute()
    assert response is not None

def test_rls_policies_enforced(supabase_client):
    """Verify RLS policies are enforced"""
    # Try to access other user's data (should fail)
    # This requires authenticated client with different user
    pass

def test_indexes_created(supabase_client):
    """Verify indexes exist for performance"""
    # Query database information_schema
    pass
```

### Step 2: Integration Tests

```python
def test_create_plan_history(supabase_client, auth_user):
    """Test creating a plan record"""
    plan = {
        'user_id': auth_user.id,
        'plan_type': 'workout',
        'plan_content': {'exercises': []},
        'duration_days': 30,
        'start_date': datetime.utcnow(),
        'end_date': datetime.utcnow() + timedelta(days=30)
    }
    
    response = supabase_client.table('plans_history').insert(plan).execute()
    assert response.data[0]['id'] is not None

def test_create_daily_progress_log(supabase_client, auth_user):
    """Test logging daily progress"""
    progress = {
        'user_id': auth_user.id,
        'log_date': date.today(),
        'weight': 75.5,
        'calories_consumed': 2100,
        'mood': 'good'
    }
    
    response = supabase_client.table('daily_progress').insert(progress).execute()
    assert response.data[0]['weight'] == 75.5

def test_gamification_points_system(supabase_client, auth_user):
    """Test points calculation"""
    # Create user points entry
    points = {
        'user_id': auth_user.id,
        'points_total': 100,
        'level': 1
    }
    
    response = supabase_client.table('user_points').upsert(points).execute()
    assert response.data[0]['points_total'] == 100
```

### Step 3: Load Testing

```python
def test_database_load():
    """Test database performance under load"""
    import time
    
    start = time.time()
    
    for i in range(1000):
        supabase_client.table('daily_progress').insert({...}).execute()
    
    duration = time.time() - start
    
    assert duration < 10  # Should complete in <10 seconds
    print(f"Insert 1000 records: {duration}s")
```

---

## Deployment Checklist

### Pre-Deployment

- [ ] Database backups created
- [ ] Migrations tested on staging environment
- [ ] TypeScript types generated and committed
- [ ] Security headers configured
- [ ] Rate limiting enabled
- [ ] Audit logging implemented
- [ ] RLS policies verified

### Deployment

- [ ] Apply migration #1: `20260319_core_data_tables.sql`
- [ ] Verify tables created (10-20 seconds)
- [ ] Apply migration #2: `20260319_favorites_achievements.sql`
- [ ] Verify tables created (10-20 seconds)
- [ ] Apply migration #3: `20260319_notifications_admin.sql`
- [ ] Verify tables created (10-20 seconds)
- [ ] Generate updated TypeScript types
- [ ] Commit TypeScript types to git
- [ ] Deploy backend with new API endpoints
- [ ] Deploy frontend with new components

### Post-Deployment

- [ ] Monitor error logs for first 24 hours
- [ ] Verify data integrity checks pass
- [ ] Test key user flows (plan creation, progress logging)
- [ ] Check database performance metrics
- [ ] Validate security headers in API responses
- [ ] Confirm audit logging is working

### Rollback Plan (If Needed)

```bash
# If issues occur, rollback to previous state:

# 1. Restore from backup
# Supabase Dashboard > Settings > Backups > Restore

# 2. Or, drop new tables via SQL:
DROP TABLE IF EXISTS plans_history CASCADE;
DROP TABLE IF EXISTS daily_progress CASCADE;
-- etc...
```

---

## Monitoring After Deployment

### Key Metrics to Watch

1. **Database Performance**
   - Query execution time (target: <100ms p95)
   - Connection pool usage
   - Index hit rates

2. **API Performance**
   - Response time (target: <150ms p95)
   - Error rate (target: <0.1%)
   - Throughput

3. **Data Integrity**
   - Duplicate record detection
   - Constraint violations
   - RLS policy violations

### Monitoring Tools

```bash
# In Supabase Dashboard:
# Analytics > Database > Query Performance
# Analytics > Database > Table Storage
# Logs > Postgres > Slow Queries

# In Application:
# Sentry > Performance > Transactions
# Datadog > Database > Query Times
```

---

## Next Steps

### Phase 1 Complete
✅ Database schema expanded (18 new tables)  
✅ TypeScript types updated  
✅ Security layer implemented  

### Phase 2 Next
🔄 API endpoint implementation (100+ endpoints)  
🔄 Frontend component creation  
🔄 Integration testing  

---

## Support & Troubleshooting

### Common Issues

**Issue**: Migration fails with "table already exists"
```sql
-- Solution: Check if table exists first
DROP TABLE IF EXISTS plans_history CASCADE;
-- Then re-run migration
```

**Issue**: RLS policies prevent data access
```sql
-- Debug: Check active policies
SELECT * FROM pg_policies WHERE tablename = 'plans_history';
-- Verify: Check policy conditions
```

**Issue**: TypeScript types out of sync
```bash
# Regenerate types
supabase gen types typescript > src/lib/database.types.ts

# Or update manually by comparing schema changes
```

---

**Document Status**: Ready for Implementation  
**Last Updated**: March 19, 2026  
**Next Review**: After Phase 1 Completion
